using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Kano : MonoBehaviour
{
    int noopoooo;
    // Start is called before the first frame update
    void Start()
    {
        noopoooo = GoE.hogehoge;
        Debug.Log(noopoooo);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
